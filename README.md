# REST API для электронной библиотеки

RESTful-сервис на базе Django и Django REST Framework для управления электронной библиотекой. Позволяет получать информацию о книгах и авторах, а также управлять каталогом (только для администраторов).

---

## 📌 Возможности

- **Просмотр книг и авторов** — для всех пользователей.
- **Поиск** по названию, жанру, автору.
- **Уникальность записей**:
  - Учебники: уникальны по (`название`, `автор`, `издательство`, `год`).
  - Художественные: уникальны по (`название`, `автор`, `издательство`).
- **Управление контентом** — только через админку или API с правами администратора.
- **Загрузка** обложек и файлов книг.

---

## 🛠 Технологии

- **Backend**: Python 3.10+, Django 4.2, Django REST Framework 3.14
- **База данных**: SQLite (по умолчанию, подходит для лабораторной работы)
- **Аутентификация**: Session + Token (через DRF)

---

## 📦 Установка и запуск

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/ваш-логин/e-library-api.git
   cd e-library-api
2. **Создайте виртуальное окружение и активируйте его**
   ```bash
   python -m venv venv
   source venv/bin/activate    # Linux/macOS
   venv\Scripts\activate       # Windows
3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
4. **Выполните миграции**
   ```bash
   python manage.py migrate
5. **Создайте суперпользователя (администратора)**
   ```bash
   python manage.py createsuperuser
6. **Запустите сервер**
   ```bash
   python manage.py runserver
